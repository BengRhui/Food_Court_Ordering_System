# OODJ ASSIGNMENT

Hi everyone! This is the repository for our Java group project.

---

## Introduction
This Java project is about a food court management system, featuring 5 different users with each assigned with distinct reponsibilities.

---

## What we learnt?
Our project has demonstrated a few programming concepts:
- Object-oriented programming concepts (*encapsulation, inheritance, abstraction, polymorphism*)
- Test plans
- GUI components (*via Java Swing and AWT components*)

---

## Running Our Project
Prerequisite before running project:
- Java Development Kit (JDK) is installed on your computer (*preferably later than JDK 21*).
- Maven is installed
  - For macOS, after installing `brew`, execute `brew install maven` on terminal
- (*Optional*) An IDE to run Java prorgams, e.g. IntelliJ IDEA / Netbeans / Visual Studio Code.

Steps to run the project:
- To run the project on Terminal (on macOS), follow these steps:
  - Download the project as a ZIP file and unzip it.
  - Via the terminal, change the directory to the location of the unzipped file using the command `cd \PATH_TO_FOLDER\`.
  - Run the following commands one-by-one:<br>
    `mvn compile`<br>
    `mvn exec:java`<br>
- The project can also be opened on an IDE and runned directly.

All credentials for login can be found at `src/main/resources/text_file/login_credentials.txt`.

---

## Documentation
To access the documentation for our project, do click on the following links:
- Assignment Requirements Document: [here](https://github.com/user-attachments/files/19758084/2411-OODJ-Assignment-QS.pdf)
- Group Report: [here](https://github.com/user-attachments/files/19758215/OODJ.Assignment_compressed.pdf)
